****-------- Funções do script --------***

*libs*
    stdlib.h - system()

1- Receber o comando no terminal com os argumentos:
    - wstaion add path "c:\1\2\3"
        Adciona um caminho para a execução de um programa
    - wstaion rm path "c:\1\2\3"
        Remove um caminho para a execução de um programa
    - wstaion start work
        Inicia a execução dos programas mapeados.
    - wstaion help
        Mostra os comandos do programa


Pseudocódigo

Verifica se o usuário digitou algum comando válido
    Se os argumentos são menores que 2 == inicia um looping avisando o usuário sobre o comando estar errado e indica o comando help

Se o usuário digitar wstation add path
    Verifica se o path é correto
    Adciona o path especificado no arquivo paths.txt

Se o usuário digitar wsation rm path
    Verifica se o path existe no arquivo paths.txt
    Exclui o caminho do arquivo.

Se o usuário digitar wstaion start work
    Percorre todo o arquivo txt com um Array que recebe cada path. exemplo = paths[3] { 0 == c:\bath\pipi.exe, 1 == c:\bath\popo.exe, 2 == c:\toddy\iup\batata.exe}
        lê os caracteres e se verificar ";" adciona o que tem antes dos pontos em path[0] e visse versa.

    Se não houver paths, alertar o usuário.
    Se não  
        Começa um looping que só termina até o último path ser executado    
    
    


